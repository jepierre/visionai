# Install the CUDA-matched PyTorch wheel separately before installing this file
# so the binary matches the NVIDIA driver on this machine.
accelerate>=1.0.0
Pillow>=10.0.0
numpy>=1.26.0
pycocotools>=2.0.7
huggingface_hub>=0.27.0
# Falcon Perception depends on the current Transformers ecosystem.
transformers>=4.45.0
falcon-perception[torch] @ git+https://github.com/tiiuae/falcon-perception.git
