# Install PyTorch separately through scripts/bootstrap.ps1 so its CUDA wheel
# matches the NVIDIA driver on the target machine.
accelerate>=1.0.0
Pillow>=10.0.0
numpy>=1.26.0
pycocotools>=2.0.7
huggingface_hub>=0.27.0
# Gemma 4 support requires the current Transformers source while upstream
# publishes a compatible stable release.
transformers @ git+https://github.com/huggingface/transformers.git
falcon-perception[torch] @ git+https://github.com/tiiuae/falcon-perception.git
